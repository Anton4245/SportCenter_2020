<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sport_center">

    <application
        android:name="ru.sport_center.App"
        android:allowBackup="true"
        android:icon="@mipmap/skysky4_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/skysky4_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.order.OrderActivity" />
        <activity android:name=".ui.intervals_сhoose.IntervalsChooseActivity" />
    </application>

</manifest>